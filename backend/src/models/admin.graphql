type Table {
  name: String!
  columns: [String!]!
}

input TableInput {
  name: String!
  columns: [String!]!
}

type Mutation {
  createTable(table: TableInput!): Boolean!
  deleteTable(name: String!): String!
}
